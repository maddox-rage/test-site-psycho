fmt.Sprintf() - ("привет, %s! тебе %d лет", name, age) - возвращает строку
reflect(package).TypeOf()
make() - инициализация слайса(make([][][] int, 10(длинна(len), 10(ёмкость, cap))))
range - для итерации по слайсу(можем получить индекс и значение соответственно)
panic() - можно самому запустить, чтобы дропнуть приложении в каких-то случиях
defer - откладывает выполнение функции на конец 
recover - позволяет восстановить приложение, если оно упало
